#pragma once

enum class [[nodiscard]] ActorSkin : unsigned char
{
    // Player
    PlayerDefault,

    // Npcs
    Bighead,

    // Projectiles
    BigBullet,

    // Props
    Prop,

    // Effects
    Explosion,
};
